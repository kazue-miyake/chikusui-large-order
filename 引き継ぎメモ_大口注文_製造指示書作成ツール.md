# 大口注文 製造指示書作成ツール 引き継ぎメモ

最終更新: 2026-05-23

## 作成したもの

大口注文用の製造指示書を作る、1ファイル完結のHTMLアプリを作成した。

作成ファイル:

`C:\Users\kazue\Documents\Codex\2026-05-23\files-mentioned-by-the-user-docx\index.html`

ブラウザで `index.html` を開くと、入力フォームとA4印刷プレビューが表示される。

## 主な機能

- お客様名、納品日、納品時間、商品名、個数を入力できる
- 納品日は印刷プレビューで曜日付き表示になる
- 納品時間は `10:00～10:45` のような自由入力
- 商品の中身写真と外装写真を登録できる
- 写真はブラウザ上で縮小・圧縮して保存する
- 材料の種類、消費期限シール、のし、輪ゴム、仕分け、箸の種類、箸の付け方、備考を入力できる
- 輪ゴムは選択式ではなく自由記述
- 保存、新規、複製、削除、入力リセット、印刷/PDF ボタンあり
- 複数の注文をブラウザの `localStorage` に保存できる
- 印刷時はA4縦1枚に収まるよう調整済み

## 印刷レイアウトの現状

PCで印刷プレビュー確認済み。

最終的に「1枚」で止まった状態。

主なレイアウト:

- 上部: タイトル、作成日、基本情報
- 中央: 商品の中身写真を大きく表示
- 下段: 材料・確認項目、右側に外装写真
- 下部: 備考
- 最下部: 確認欄

確認欄:

- 食材発注
- 包材発注
- 受注
- 調理
- 盛付
- 最終

確認欄は印鑑を押せるようにしたが、白紙2枚目対策のため最終的に高さを少し抑えた。

## 調整で苦労した点

印刷時に白紙の2枚目が出る問題があった。

最終対応:

- 印刷時の外側コンテナを印刷フローから外す
- `.paper` を印刷対象として絶対配置
- 紙面高さを短めに調整
- 確認欄を `20mm` から `16mm` に縮小

この状態でユーザー確認により「1枚で止まった」と確認済み。

## 次のミッション

スマホで開けるようにする。

推奨方針:

GitHub Pagesで公開する。

理由:

- 既存の発注管理アプリと同じ運用にできる
- スマホでURLを開くだけで使える
- サーバー不要
- `index.html` だけで公開できる

次にやること:

1. GitHubで新しいリポジトリを作る
   - 例: `chikusui-large-order`
2. この `index.html` をリポジトリのルートにアップロードする
3. GitHub Pagesを有効化する
   - Settings -> Pages
   - Deploy from a branch
   - Branch: `main`
   - Folder: `/root`
4. 公開URLをスマホで開く
5. スマホで入力、写真登録、保存、印刷/PDF導線を確認する

## 注意点

- `localStorage` 保存なので、PCとスマホでデータは共有されない
- スマホで入力した内容はスマホのブラウザ内に保存される
- 複数端末で同じ注文データを共有したい場合は、将来的にサーバー化やAppSheet連携が必要
- 写真を多く保存すると `localStorage` 容量に近づく可能性がある
- 初回公開後、スマホ表示でフォームや印刷が崩れる場合はCSS調整が必要

## 次回Codexへの依頼文例

「この引き継ぎメモと `index.html` を見て、スマホで開けるようにGitHub Pages公開の準備を手伝って」

または

「`index.html` をスマホ公開用に整えて、GitHub Pagesへ上げる手順を案内して」
